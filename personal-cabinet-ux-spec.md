# UX/UI спецификация личного кабинета

## 1. Цель продукта

Личный кабинет помогает пользователю пройти путь от фото до готового персонального мультфильма без ощущения сложного генеративного сервиса.

Главная продуктовая механика:

1. Пользователь загружает фото и заполняет базовые данные.
2. Сервис бесплатно создает 3 мультяшных образа героя.
3. Пользователь выбирает понравившийся образ.
4. Только после выбора образа пользователь оплачивает конкретный мультфильм.
5. После оплаты заказ переходит в создание, затем становится доступен просмотр и скачивание.

Запрещенная механика:

- не использовать внутреннюю валюту;
- не продавать пакеты генераций;
- не просить оплату до выбора образа;
- не делать интерфейс похожим на крипто-кошелек, биржу или AI-dashboard.

Основной тон интерфейса: теплый, премиальный, семейный, мягкий, понятный.

## 2. Информационная архитектура

Нижнее меню фиксированное, mobile-first, 5 разделов:

| Раздел | Назначение | Главный CTA |
| --- | --- | --- |
| Создать | Новый мультфильм по фото | Создать 3 образа бесплатно |
| Мои работы | Заказы и статусы | Действие зависит от статуса |
| Примеры | Галерея готовых мультфильмов | Смотреть пример |
| Подарок | Покупка сертификата | Купить сертификат |
| Профиль | Данные, оплаты, помощь | Написать в поддержку |

Первичный пользовательский flow:

```text
Вход
  -> Создать
  -> Создание 3 образов
  -> Выбор образа
  -> Оплата конкретного мультфильма
  -> Создается
  -> Готово
  -> Смотреть / Скачать
```

Возвратный пользователь чаще начинает с `Мои работы`, если у него есть незавершенные заказы.

## 3. Экран входа

Задача: мягко ввести пользователя в кабинет и объяснить ценность без сложной регистрации.

Структура:

- Фон: кремово-персиковый с легкими бликами.
- Верхний бейдж: `Личный кабинет`.
- Заголовок: `Создайте мультфильм по фото`.
- Подзаголовок: `Загрузите фото, выберите героя и оплатите только понравившийся результат.`
- Поле входа:
  - вариант 1: `Телефон или email`;
  - placeholder: `+7 999 123-45-67`;
  - helper text: `Пришлем код для входа.`
- Primary CTA: `Войти и продолжить`.
- Secondary link: `Посмотреть примеры`.

Состояния:

- Пустое поле: CTA disabled.
- Некорректный формат: текст ошибки `Проверьте телефон или email`.
- Код отправлен: экран/модалка с вводом 4-6 цифр.
- Первый вход: после успешного входа открыть вкладку `Создать`.

## 4. Экран `Создать`

Задача: собрать минимум данных для генерации 3 бесплатных образов.

Верх экрана:

- Заголовок: `Новый мультфильм`.
- Подзаголовок: `Сначала создадим 3 образа героя бесплатно. Оплата будет только после выбора.`
- Небольшой прогресс: `Шаг 1 из 3`.

Блок загрузки фото:

- Большая upload-card на всю ширину.
- Текст: `Загрузите фото героя`.
- Подсказка: `Лучше подойдет четкое фото лица при хорошем освещении.`
- CTA внутри зоны: `Выбрать фото`.
- После загрузки показывать превью, кнопку `Заменить`, маленький статус `Фото загружено`.

Поля:

- `Имя героя`
  - placeholder: `Например, Лиза`;
  - helper text: `Так мы будем обращаться к герою в сценарии.`
- `Возраст`
  - input/stepper;
  - варианты быстрых чипов: `3-5`, `6-8`, `9-12`, `Взрослый`.

Выбор повода:

- Чипы или cards 2x2:
  - `Для ребенка`;
  - `История любви`;
  - `День рождения`;
  - `Свадьба`.
- Выбранный повод получает золотистую рамку и мягкое свечение.

Выбор сюжета:

- Не dropdown.
- Карточки с коротким описанием:
  - `Волшебное приключение` — `Герой попадает в сказочный мир и находит чудо.`
  - `Теплая история` — `Спокойный мультфильм про семью, любовь и заботу.`
  - `Праздничный сюрприз` — `Поздравление с подарками, свечами и финальным вау-моментом.`
  - `Романтическая история` — `Мультфильм про знакомство, чувства и общий финал.`

Нижняя sticky-зона:

- Primary CTA: `Создать 3 образа бесплатно`.
- Trust text под кнопкой: `Оплата только после выбора образа`.

Валидация:

- Без фото CTA disabled.
- Без имени показывать helper: `Добавьте имя героя`.
- Без повода и сюжета CTA disabled.
- Ошибка загрузки: `Не удалось загрузить фото. Попробуйте другое изображение.`

## 5. Состояние генерации образов

Задача: показать, что сервис работает, но не обещать точное время.

Экран/полноэкранная карточка:

- Иллюстративный loader: мягкое свечение, звезды, мини-превью фото.
- Заголовок: `Создаем 3 образа героя`.
- Подзаголовок: `Обычно это занимает пару минут.`
- Прогресс без процентов:
  - `Анализируем фото`;
  - `Подбираем мультяшный стиль`;
  - `Готовим варианты героя`.

Ошибка:

- Заголовок: `Не получилось создать образы`.
- Текст: `Фото могло быть слишком темным или размытым. Загрузите другое изображение.`
- CTA: `Попробовать еще раз`.
- Secondary: `Заменить фото`.

## 6. Экран выбора образа

Задача: дать пользователю эмоционально выбрать героя перед оплатой.

Структура:

- Заголовок: `Выберите героя для мультфильма`.
- Подзаголовок: `Мы бесплатно подготовили 3 образа. Выберите тот, который нравится больше всего.`
- Карточки аватаров вертикальным списком.

Карточка образа:

- Большое изображение 3:4 или 4:5.
- Название стиля:
  - `Сказочный`;
  - `Праздничный`;
  - `Кинематографичный`.
- Короткое описание: `Мягкий стиль с теплым светом и крупными эмоциями.`
- При выборе:
  - золотистая рамка 2px;
  - мягкое внешнее свечение;
  - чек-иконка в правом верхнем углу;
  - микротекст: `Выбран для мультфильма`.

Нижняя sticky-зона:

- До выбора: disabled CTA `Выберите образ`.
- После выбора: primary CTA `Продолжить с этим образом`.
- Под CTA: `Следующий шаг — оплата конкретного мультфильма.`

Правило:

- Один набор из 3 образов бесплатно на один черновик.
- Не показывать бесплатную перегенерацию на этом этапе.

## 7. Экран оплаты

Задача: прозрачно объяснить, за что пользователь платит, и запустить CloudPayments.

Структура:

- Заголовок: `Ваш мультфильм почти готов`.
- Подзаголовок: `Вы выбрали героя. Теперь оплатите создание одного мультфильма.`
- Карточка выбранного образа:
  - превью героя;
  - имя героя;
  - повод;
  - сюжет.
- Карточка цены:
  - крупно: `1 490 ₽` или `1 990 ₽`;
  - подпись: `Цена за 1 готовый мультфильм`;
  - бейдж: `Без подписок`;
  - бейдж: `Без дополнительных платежей`.
- Блок результата:
  - `Готовый короткий мультфильм`;
  - `Персональный герой по выбранному образу`;
  - `Ссылка для просмотра и скачивания`;
  - `Срок создания: 5-20 минут`.
- Primary CTA: `Оплатить и создать мультфильм`.
- Secondary link: `Вернуться к выбору образа`.

CloudPayments UX:

- По клику открывается платежный виджет CloudPayments.
- Если оплата успешна:
  - заказ получает статус `Создается`;
  - пользователь попадает в `Мои работы` на карточку заказа.
- Если пользователь закрыл виджет:
  - статус остается `Ожидает оплаты`;
  - экран оплаты показывает notice `Оплата не завершена`.
- Если ошибка:
  - notice `Оплата не прошла. Деньги не списаны.`
  - CTA остается `Попробовать оплатить еще раз`.

## 8. Экран `Мои работы`

Задача: показать все заказы и дать одно понятное действие на каждый.

Верх:

- Заголовок: `Мои работы`.
- Подзаголовок: `Здесь хранятся черновики, выбранные герои и готовые мультфильмы.`
- Empty state:
  - `У вас пока нет мультфильмов`;
  - CTA: `Создать первый мультфильм`.

Карточка заказа:

- Превью: фото/образ/постер.
- Название: `Лиза и волшебный лес`.
- Мета: `Для ребенка · 28 апреля`.
- Статус-чип.
- Главное действие.
- Второстепенное действие только для готового заказа.

Матрица статусов:

| Статус | Что значит | Главное действие | Доп. действие |
| --- | --- | --- | --- |
| Черновик | Пользователь начал форму, но не создал образы | Продолжить | Удалить |
| Образы готовы | 3 образа созданы, выбор не сделан | Выбрать героя | - |
| Ожидает оплаты | Герой выбран, оплата не завершена | Оплатить | Изменить образ |
| Создается | Оплата прошла, мультфильм в работе | В процессе | - |
| Готово | Мультфильм доступен | Смотреть | Скачать |

Важное правило:

- В каждой карточке должен быть один главный CTA.
- Не перегружать карточки несколькими равнозначными кнопками.

## 9. Экран `Примеры`

Задача: вдохновить пользователя и помочь выбрать формат.

Верх:

- Заголовок: `Примеры работ`.
- Подзаголовок: `Посмотрите, какими могут быть персональные мультфильмы.`

Категории:

- Горизонтальные чипы:
  - `Все`;
  - `Для ребенка`;
  - `История любви`;
  - `День рождения`;
  - `Свадьба`.

Карточки:

- Большое изображение/постер.
- Категория в светлом pill.
- Название:
  - `Лиза и волшебный лес`;
  - `Ты — моя вселенная`;
  - `Праздник-сюрприз`;
  - `Свадебная история`.
- CTA: `Смотреть пример`.

Поведение:

- На mobile карточки идут вертикальным списком.
- Можно использовать одну featured-карточку сверху.
- После просмотра примера предлагать CTA: `Создать похожий мультфильм`.

## 10. Экран `Подарок`

Задача: продать сертификат на мультфильм как подарок.

Верх:

- Заголовок: `Подарочный сертификат`.
- Подзаголовок: `Подарите близкому человеку персональный мультфильм.`

Форма:

- Выбор формата:
  - `Для ребенка` — `1 490 ₽`;
  - `Для пары` — `1 990 ₽`;
  - `Для праздника` — `1 490 ₽`;
  - `Свадьба` — `1 990 ₽`.
- Поле: `Имя получателя`.
- Поле: `Ваше поздравление`.
- Опционально: `Дата вручения`.

CTA:

- `Купить сертификат`.

После оплаты:

- Экран результата:
  - `Сертификат готов`;
  - код сертификата;
  - ссылка;
  - кнопка `Скопировать ссылку`;
  - кнопка `Отправить`.

Важно:

- Сертификат не должен выглядеть как промокод на скидку.
- Это право получить один конкретный мультфильм выбранного формата.

## 11. Экран `Профиль`

Задача: дать пользователю контроль и доверие.

Верх:

- Аватар/инициалы.
- Имя пользователя.
- Телефон/email.

Разделы:

- `История оплат`;
- `Помощь`;
- `Конфиденциальность фото`;
- `Условия возврата`;
- `Выйти`.

Блок доверия:

- Заголовок: `Ваши фото защищены`.
- Текст: `Мы используем изображения только для создания вашего мультфильма и не показываем их другим пользователям.`
- CTA/link: `Подробнее о конфиденциальности`.

## 12. Визуальная система

Фон:

- Светлый кремово-персиковый.
- Мягкие блики, звезды, легкая бумажная текстура.
- Без темного вертикального градиента.

Типографика:

- Заголовки: `Cormorant Garamond`, 600-700.
- Интерфейсный текст: `Manrope`, 400-700.
- Основной цвет текста: теплый темно-коричневый.
- Вторичный текст: коричневый с прозрачностью 65-75%.

Карточки:

- Background: полупрозрачный кремовый.
- Border: тонкий золотисто-персиковый.
- Radius: 24-32px.
- Shadow: мягкая коричнево-золотистая, без жесткой черной тени.

Кнопки:

- Primary:
  - кремово-золотой фон;
  - темно-коричневый текст;
  - border 1px теплый золотистый;
  - shadow вниз и чуть в сторону.
- Secondary:
  - темный графит/какао;
  - белый текст;
  - мягкая объемная тень.
- Disabled:
  - светлый кремовый;
  - muted text;
  - без свечения.

Статусы:

- `Черновик`: светлый кремовый.
- `Образы готовы`: персиковый.
- `Ожидает оплаты`: золотистый.
- `Создается`: теплый лавандово-персиковый.
- `Готово`: мягкий зеленый.

Нижнее меню:

- Fixed bottom.
- Высота 72-82px + safe-area.
- Фон: полупрозрачный кремовый glass.
- Активный пункт: золотистый pill.
- Иконки простые, rounded, без техно-стиля.

## 13. Mobile-first правила

- Основной viewport: 390px.
- Максимальная ширина интерфейса: 430px.
- На desktop показывать мобильный кабинет по центру.
- Все основные CTA должны быть доступны большим пальцем.
- Sticky bottom CTA использовать на ключевых шагах: `Создать`, `Выбор образа`, `Оплата`.
- Не делать плотные таблицы в интерфейсе; матрицы статусов нужны только в спецификации.
- Списки карточек вертикальные, кроме маленьких чипов и категорий.

## 14. Acceptance criteria

- Пользователь понимает до оплаты: фото -> 3 бесплатных образа -> выбор -> оплата.
- На экранах нет внутренней валюты, баланса и пакетов генераций.
- Цена на оплате всегда относится к одному конкретному мультфильму.
- CloudPayments запускается только после выбора образа.
- В `Мои работы` каждая карточка имеет понятный статус и одно главное действие.
- На ширине 390px нет горизонтального скролла.
- Стиль визуально светлый, семейный и премиальный.
- Интерфейс не похож на crypto-app, AI-dashboard или банковское приложение.

## 15. Рекомендуемый порядок прототипирования

1. Сделать кликабельный mobile-flow: `Вход -> Создать -> Выбор образа -> Оплата`.
2. Добавить `Мои работы` со всеми статусами.
3. Добавить `Примеры`.
4. Добавить `Подарок`.
5. Добавить `Профиль`.
6. Проверить весь flow на ширине 390px и 430px.
